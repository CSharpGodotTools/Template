name: PR CI

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened

permissions:
  contents: read

jobs:
  verify:
    name: Build and test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: |
            10.0.x
            8.0.x

      - name: Detect changed projects
        id: changes
        shell: bash
        run: |
          set -euo pipefail

          BASE_SHA="${{ github.event.pull_request.base.sha }}"
          HEAD_SHA="${{ github.event.pull_request.head.sha }}"
          mapfile -t CHANGED_PATHS < <(git diff --name-only "$BASE_SHA" "$HEAD_SHA")

          echo "Changed files:"
          printf '%s\n' "${CHANGED_PATHS[@]}"

          build_godotutils=false
          build_visualize=false
          build_packetgen=false
          build_optionsgen=false
          build_template=false

          declare -a changed_dotnet=()

          for changed_path in "${CHANGED_PATHS[@]}"; do
            [[ -z "$changed_path" ]] && continue

            case "$changed_path" in
              Template.GodotUtils/*)
                build_godotutils=true
                ;;
              Template.Visualize/*)
                build_visualize=true
                ;;
              Template.PacketGen/*)
                build_packetgen=true
                ;;
              Template.OptionsGen/*)
                build_optionsgen=true
                ;;
              Template/*)
                build_template=true
                ;;
            esac

            case "$changed_path" in
              *.cs|*.csproj|*.sln|*.props|*.targets|global.json|NuGet.config|nuget.config|*/NuGet.config|*/nuget.config|.github/workflows/*.yml|.github/workflows/*.yaml)
                changed_dotnet+=("$changed_path")
                ;;
            esac
          done

          if $build_visualize; then
            build_godotutils=true
          fi

          changed_lines="$(printf '%s\n' "${CHANGED_PATHS[@]}")"

          if echo "$changed_lines" | grep -Eq "^(Template\.sln|\.editorconfig|NuGet\.config|\.githooks/|\.github/workflows/)"; then
            build_godotutils=true
            build_visualize=true
            build_packetgen=true
            build_optionsgen=true
            build_template=true
          fi

          if ! $build_godotutils && ! $build_visualize && ! $build_packetgen && ! $build_optionsgen && ! $build_template; then
            echo "No known project paths changed. Falling back to full validation."
            build_godotutils=true
            build_visualize=true
            build_packetgen=true
            build_optionsgen=true
            build_template=true
          fi

          has_dotnet_changes=false
          if [[ "${#changed_dotnet[@]}" -gt 0 ]]; then
            has_dotnet_changes=true
          fi

          {
            echo "build_godotutils=$build_godotutils"
            echo "build_visualize=$build_visualize"
            echo "build_packetgen=$build_packetgen"
            echo "build_optionsgen=$build_optionsgen"
            echo "build_template=$build_template"
            echo "has_dotnet_changes=$has_dotnet_changes"
          } >> "$GITHUB_OUTPUT"

      - name: Seed local package feed
        if: steps.changes.outputs.has_dotnet_changes == 'true'
        shell: bash
        run: |
          set -euo pipefail

          mkdir -p ./Template/Framework/Libraries
          rm -f ./Template/Framework/Libraries/*.nupkg

          dotnet pack Template.GodotUtils/GodotUtils.csproj \
            -c Release \
            --output ./Template/Framework/Libraries

          dotnet build Template.Visualize/Visualize.csproj \
            -c Release

          dotnet pack Template.Visualize/Visualize.csproj \
            -c Release \
            --no-build \
            --output ./Template/Framework/Libraries

          dotnet pack Template.PacketGen/PacketGen/PacketGen.csproj \
            -c Release \
            --output ./Template/Framework/Libraries

          dotnet pack Template.OptionsGen/OptionsGen/OptionsGen.csproj \
            -c Release \
            --output ./Template/Framework/Libraries

      - name: Restore
        if: steps.changes.outputs.has_dotnet_changes == 'true'
        run: dotnet restore Template.sln

      - name: Skip .NET validation when no .NET changes
        if: steps.changes.outputs.has_dotnet_changes != 'true'
        run: echo "No .NET-impacting changes detected. Skipping .NET build/test validation."

      - name: Targeted builds
        if: steps.changes.outputs.has_dotnet_changes == 'true'
        shell: bash
        run: |
          set -euo pipefail

          if [[ "${{ steps.changes.outputs.build_godotutils }}" == "true" ]]; then
            dotnet build Template.GodotUtils/GodotUtils.csproj --no-restore -warnaserror
          fi

          if [[ "${{ steps.changes.outputs.build_visualize }}" == "true" ]]; then
            dotnet build Template.Visualize/Visualize.csproj --no-restore -warnaserror
          fi

          if [[ "${{ steps.changes.outputs.build_packetgen }}" == "true" ]]; then
            dotnet build Template.PacketGen/PacketGen/PacketGen.csproj -c Release --no-restore -warnaserror
          fi

          if [[ "${{ steps.changes.outputs.build_optionsgen }}" == "true" ]]; then
            dotnet build Template.OptionsGen/OptionsGen/OptionsGen.csproj -c Release --no-restore -warnaserror
          fi

          if [[ "${{ steps.changes.outputs.build_template }}" == "true" ]]; then
            dotnet build Template/Template.csproj -warnaserror
          fi

      - name: Full solution build
        if: steps.changes.outputs.has_dotnet_changes == 'true'
        run: dotnet build Template.sln -warnaserror

      - name: Tests
        if: steps.changes.outputs.has_dotnet_changes == 'true'
        run: dotnet test Template.PacketGen/PacketGen.Tests/PacketGen.Tests.csproj --no-build
